用于存放代码中公共部分，也就是经常出现的代码块，可以封装在一个文件中，只需一行PHP代码即可调用，需要实现的功能也就只要写一次，避免代码的冗余，例如一页的页脚和头部基本上都是一样的，这样就可以封装问head和footer文件，在头部用require_once ".admin/public/head.php";调用即可，其功能就是将该文件中的代码引用的此处，和在这里面也是一样的，但是特别注意的是文件路径的问题，路径是对于引用的文件，而不是封装的文件路径，这个问题可以用绝对路径path来解决！
